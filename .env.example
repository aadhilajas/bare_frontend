# Backend API base URL (baked in at build time).
#
# Production (Vercel):
VITE_CRM_API_URL=https://barebackend-production.up.railway.app
#
# Local dev: leave unset or use localhost — Vite proxy forwards /api to port 8000.
# VITE_CRM_API_URL=http://localhost:8000
